# AI 网关项目审视与创新补充

> 审视日期：2026-07-30  
> 作用：记录为什么调整项目定位、哪些能力进入 MVP、哪些能力延后，以及后续设计评审时必须守住的边界。

## 1. 审视结论

现有项目总纲覆盖面完整，但初版如果直接实施，很容易变成“功能很多、关键链路不够可靠”的平台。市场上的 AI 网关已经普遍具备统一协议、虚拟 Key、限流、简单路由和成本看板，因此本项目需要形成更鲜明的技术主线。

最终确定的四个核心差异点：

1. **流式调用可核算**：即使请求部分输出、客户端取消或发生多次上游尝试，也能形成完整、幂等、可对账的用量账本。
2. **并发预算不超卖**：使用预算预留与结算，而不是请求结束后才累计消费。
3. **协议适配可验证**：用一致性 Fixture 验证缓存 Token、工具调用、错误和流事件，而不是靠人工试调用。
4. **路由决策可解释**：记录候选过滤、能力、健康、质量、价格和策略版本，支持复盘。

MCP/A2A 工具治理与自托管模型推理感知路由很有创新价值，但不进入第一版关键路径。

## 2. 当前市场能力已经比较成熟的部分

以下能力属于“必须有，但仅实现它们不足以形成特色”：

- OpenAI-compatible API。
- 多供应商模型适配。
- 虚拟 API Key。
- RPM/TPM 限流。
- 简单负载均衡与故障切换。
- 调用日志、Token 与成本看板。
- 基础内容过滤和审计。

这些能力应按工程标准实现，但不能作为项目最主要的创新宣传点。

## 3. 仍然困难且值得投入的痛点

| 痛点 | 为什么难 | 本项目方案 | 优先级 |
|---|---|---|---|
| 流式部分成功 | 输出、计费、取消和重试状态不一致 | Request/Attempt/Segment 事实模型 | P0 |
| 缓存 Token 计价 | 供应商字段和折扣语义不同 | NormalizedUsage + 价格类型版本 | P0 |
| 并发预算超卖 | 多请求同时通过旧余额检查 | 原子预算预留与结算 | P0 |
| 协议漂移 | 新字段静默丢失，测试覆盖不足 | Adapter Conformance Suite | P0 |
| 路由质量下降 | “能力相同”不代表效果相同 | 质量门禁、shadow、可解释决策 | P1 |
| 账单不一致 | 失败、缓存、区域和折扣复杂 | 供应商账单对账 | P1 |
| 缓存越权/过期 | 权限、知识和策略持续变化 | 版本化、安全缓存 Key | P1 |
| MCP 工具副作用 | 工具可读写真实系统 | 工具级权限、审批、幂等、预算 | P2 |
| Agent 循环 | Agent 间调用可能无限放大 | 最大步骤/成本/工具次数 | P2 |
| GPU 路由效率 | 轮询不了解 KV Cache/LoRA/队列 | Inference Endpoint Picker | P2 |

## 4. 最重要的架构调整

### 4.1 从“先拆微服务”改为“三进程模块化架构”

第一版只实现：

- `gateway`：数据面、认证、策略、路由、代理和流式处理。
- `control-plane`：租户、项目、Key、模型、部署、价格和策略管理。
- `metering-worker`：用量消费、账本、结算和分析写入。

三者同仓库、共享领域类型，但保持清晰依赖方向。只有当某模块出现独立扩容、隔离或团队边界时才拆服务。

### 4.2 从“请求一条记录”改为“不可变事实账本”

一次请求可能包含多个上游 Attempt。费用不是请求表上的一个可覆盖字段，而是多条不可变 Usage Ledger 分录的合计。

好处：

- 可以解释重试为什么产生额外费用。
- 部分流失败不会丢掉已经产生的 Token。
- 价格或 Token 修正通过 adjustment 分录完成，审计链不被破坏。
- 更适合供应商账单对账。

### 4.3 从“适配器能调用”改为“适配器必须通过契约测试”

每个 Provider Adapter 在注册/发布前运行一致性测试：

- 非流式正常响应。
- SSE 正常结束。
- 首包前失败与首包后失败。
- 客户端取消。
- 输入、输出、缓存读、缓存写和推理 Token。
- Tool Call/结构化输出。
- 429、5xx、认证和上下文过长错误。
- 未知字段和供应商协议升级。

## 5. MVP 决策

### 5.1 MVP 必须做

- Go 数据面与 OpenAI-compatible Chat API。
- 普通响应、SSE、Context 取消和背压。
- Mock Provider + 一个真实 Provider Adapter。
- 租户、项目、虚拟 Key 和模型白名单。
- 固定/优先级路由、健康、超时、熔断和首包前重试。
- RPM、TPM、并发限制。
- 预算预留、Attempt 用量和不可变账本。
- 配置版本、审计、OpenTelemetry 和核心 Dashboard。
- 一致性、故障、并发预算和流式测试。

### 5.2 MVP 不做

- 不适配大量供应商。
- 不做语义缓存。
- 不做在线自动调权。
- 不执行 MCP 工具。
- 不接真实 GPU 推理集群。
- 不做复杂企业前端，先完成必要控制台。

### 5.3 MVP 完成定义

只有同时满足以下条件才能标记 MVP 完成：

- 关键测试全部自动化，可重复运行。
- 客户端取消能释放上游连接。
- 流式部分失败仍产生用量分录。
- 预算并发测试不超卖。
- 重复事件不重复计费。
- 控制面短时故障不影响数据面使用最近配置。
- 默认日志和 Trace 不包含 Prompt、Response 和密钥。
- 有完整 Docker Compose、API 文档、压测报告与演示脚本。

## 6. 创新能力的实施顺序

### 第一层：正确性创新

先解决协议、流式、预算和账本正确性。这一层对真实企业最有价值，也最经得住面试追问。

### 第二层：治理创新

增加账单对账、质量门禁、配置灰度、模型生命周期、安全缓存与数据驻留策略。

### 第三层：Agent 与推理基础设施

增加 MCP/A2A 工具治理、Agent 循环保护，以及 KV-cache/LoRA/队列感知路由。

三层不得倒序。未解决账本和预算正确性前，不开始语义缓存或复杂智能路由。

## 7. 技术选型结论

| 项目 | 选择 |
|---|---|
| 主要语言 | Go 1.26（本机固定 1.26.5） |
| HTTP | 标准 `net/http` + chi；SSE 核心尽量使用标准能力 |
| 配置/控制面 | 同仓库独立进程 |
| 数据库 | PostgreSQL + pgx/sqlc |
| 缓存与限流 | Redis + Lua |
| 事件 | Redpanda/Kafka |
| 分析 | ClickHouse |
| 遥测 | OpenTelemetry、Prometheus、Grafana |
| 本地环境 | Docker Compose |
| 部署演示 | Kubernetes + Helm/Kustomize |

选择 Go 的主要理由是：流式代理、Context 取消、并发连接和资源控制更适合展示后端能力。核心链路不引入过重框架，方便理解每个超时和连接行为。

## 8. 关键设计原则

1. **数据库事实优先**：预算、账本和配置版本以 PostgreSQL 为事实源。
2. **热路径最小化**：Data Plane 不同步写 ClickHouse，不在每个流 Chunk 查 Redis。
3. **至少一次 + 幂等**：用量事件允许重复投递，计量服务必须幂等。
4. **不承诺无法保证的 exactly-once**。
5. **首包后不透明切换模型**。
6. **默认不保存内容**：Prompt/Response 留存需要租户显式策略。
7. **质量门槛高于成本优化**。
8. **AI 不参与认证、预算和权限的最终决策**。
9. **配置和价格必须版本化**。
10. **每个创新点必须有可自动化的验收测试**。

## 9. 参考项目与调研依据

- [LiteLLM](https://github.com/BerriAI/litellm)：统一协议、多供应商、成本、路由、MCP/A2A。
- [Envoy AI Gateway](https://github.com/envoyproxy/ai-gateway)：两层网关与云原生数据面。
- [Kubernetes Gateway API Inference Extension](https://github.com/kubernetes-sigs/gateway-api-inference-extension)：KV-cache、请求成本、LoRA 与队列感知路由。
- [agentgateway](https://github.com/agentgateway/agentgateway)：LLM、MCP、A2A 统一治理。
- [Apache APISIX](https://github.com/apache/apisix)：动态路由、插件、认证、限流与可观测性。
- [Envoy Proxy](https://github.com/envoyproxy/envoy)：高性能代理、连接、重试与熔断语义。
- [Langfuse](https://github.com/langfuse/langfuse)：LLM Trace、Prompt 版本、反馈与成本。

用于测试设计的公开问题线索：

- [缓存 Token 跨协议计价问题](https://github.com/BerriAI/litellm/issues/35127)
- [流式调用日志回调缺失](https://github.com/BerriAI/litellm/issues/35124)
- [Prompt Cache 成本偏差](https://github.com/BerriAI/litellm/issues/34801)
- [流中回退丢失部分用量](https://github.com/BerriAI/litellm/issues/33273)
- [并发请求绕过会话预算](https://github.com/BerriAI/litellm/issues/34732)
- [项目预算缺少原子预留](https://github.com/BerriAI/litellm/issues/34101)

这些链接用于识别风险和构造测试，不用于评价对应项目整体质量。

## 10. 下一步

所有实现任务以同目录下的《开发执行清单.md》为唯一进度来源。开始开发后：

- 每完成一项，将 `[ ]` 改为 `[x]` 并填写完成日期和证据。
- 正在执行的任务标记为 `[~]`。
- 阻塞任务标记为 `[!]`，写明原因和解除条件。
- 不跨过阶段门禁；若调整顺序，先更新清单和 ADR。
