# AI 网关与模型成本治理平台——开发执行清单

> 本文件是项目唯一进度清单（Single Source of Truth）。后续开发必须按依赖顺序执行；每完成一项立即更新状态、日期和验证证据，避免遗漏。

## 0. 使用规则

### 0.1 状态标记

- `[ ]`：尚未开始。
- `[~]`：正在进行，同一时间原则上只允许一个主任务处于此状态。
- `[x]`：已经完成，并通过该任务的验收条件。
- `[!]`：阻塞，必须填写阻塞原因、影响和解除条件。
- `[-]`：经 ADR 明确取消，不得直接删除任务。

### 0.2 更新格式

完成任务后，在任务末尾追加：

```text
完成：YYYY-MM-DD
证据：测试命令、报告路径、接口截图、提交号或文档链接
```

不得因为“代码写完”就标记完成，必须满足验收条件。测试失败、文档缺失或存在未处理安全问题时，任务保持 `[~]`。

### 0.3 当前状态

| 项目 | 当前值 |
|---|---|
| 当前阶段 | P03 应用骨架与横切能力 |
| 当前主任务 | P03-T04 实现配置加载与热配置快照接口 |
| MVP 状态 | 未开始 |
| 阻塞项 | 无 |
| 最后更新 | 2026-07-30 |

### 0.4 阶段原则

1. P00～P03 为工程地基，不完成不进入业务功能。
2. P04～P13 构成 P0/MVP 核心链路。
3. P14～P18 为 P1 差异化与上线质量。
4. P19～P20 为 P2 高级创新扩展。
5. P21 为最终交付、演示和简历材料。
6. 若改变技术方案或阶段顺序，必须先新增/更新 ADR，再修改本清单。

---

## P00 项目治理与准备

目标：建立稳定的项目目录、文档、决策记录和完成标准。本阶段不编写业务代码。

- [x] **P00-T01 建立桌面项目根目录**
  - 产物：`C:\Users\Administrator\Desktop\ai-gateway-platform`。
  - 验收：目录存在，项目文件后续统一保存在该目录。
  - 完成：2026-07-30
  - 证据：桌面目录及 `项目开发总纲.md`。

- [x] **P00-T02 迁入并二次审视项目总纲**
  - 产物：`项目开发总纲.md`、`项目审视与创新补充.md`。
  - 验收：补充流式计量、预算预留、适配器契约、账单对账、MCP/A2A 和推理感知路由。
  - 完成：2026-07-30
  - 证据：上述两份文档。

- [x] **P00-T03 建立开发执行清单**
  - 产物：本文件。
  - 验收：任务按依赖排序，具有状态、产物和验收条件。
  - 完成：2026-07-30
  - 证据：`开发执行清单.md`。

- [x] **P00-T04 建立项目索引 README**
  - 产物：根目录 `README.md`。
  - 内容：项目定位、核心特色、目录索引、快速开始占位、当前阶段和许可证说明。
  - 验收：新参与者可以从 README 找到总纲、执行清单、架构文档和启动方式。
  - 完成：2026-07-30
  - 证据：项目根目录 `README.md`。

- [x] **P00-T05 初始化 Git 仓库与忽略规则**
  - 产物：`.git`、`.gitignore`、`.gitattributes`、`.editorconfig`。
  - 规则：禁止提交 `.env`、密钥、数据库数据目录、IDE 临时文件和压测大文件。
  - 验收：`git status` 只显示预期文件；密钥扫描测试文件不会被提交。
  - 完成：2026-07-30
  - 证据：桌面项目已初始化 `main` Git 仓库；`git check-ignore` 验证 `.env`、`secrets/provider.key`、`data/local.db` 均被忽略。

- [x] **P00-T06 建立文档与决策目录**
  - 产物：`docs/architecture`、`docs/adr`、`docs/api`、`docs/security`、`docs/runbook`、`docs/reports`。
  - 验收：每类文档有 README 说明命名规则和维护责任。
  - 完成：2026-07-30
  - 证据：`docs/README.md` 及各子目录 README。

- [x] **P00-T07 固化开发语言与工具版本**
  - 产物：`docs/architecture/technology-stack.md` 或 ADR。
  - 建议：Go 1.26、PostgreSQL、Redis、Redpanda/Kafka、ClickHouse、OpenTelemetry。
  - 验收：记录版本、选择理由、替代方案和升级策略。
  - 完成：2026-07-30
  - 证据：`docs/architecture/technology-stack.md`；Go 1.26.5 已校验官方 SHA-256 后安装，Docker 仍待安装，相关验收未提前标记。

- [x] **P00-T08 定义项目完成标准 Definition of Done**
  - 产物：`docs/development/definition-of-done.md`。
  - 至少包含：代码、测试、文档、安全、遥测、迁移、回滚和性能证据。
  - 验收：后续每个阶段门禁可以引用统一标准。
  - 完成：2026-07-30
  - 证据：`docs/development/definition-of-done.md`。

- [x] **P00-T09 建立变更日志与版本策略**
  - 产物：`CHANGELOG.md`、版本约定文档。
  - 验收：明确 API、事件和数据库 Schema 的兼容性规则。
  - 完成：2026-07-30
  - 证据：`CHANGELOG.md`、`docs/development/versioning.md`。

### P00 阶段门禁

- [x] 文档索引完整。
- [x] Git 仓库没有敏感文件。
- [x] 技术栈、版本策略和完成标准已经明确。

---

## P01 产品边界、架构与安全基线

目标：在编码前确定 MVP、关键语义、SLO、威胁和领域模型，减少后期返工。

- [x] **P01-T01 固化 MVP 用户故事与非目标**
  - 产物：`docs/product/mvp-scope.md`。
  - 内容：平台管理员、租户管理员、开发者三个角色；两个 Provider；Chat 普通/流式；Key、路由、限流、预算和计量。
  - 验收：所有需求均标记 P0/P1/P2，P0 不包含 MCP、语义缓存和 GPU 路由。
  - 完成：2026-07-30
  - 证据：`docs/product/mvp-scope.md`。

- [x] **P01-T02 定义系统上下文与信任边界**
  - 产物：系统上下文图、数据流图。
  - 内容：客户端、网关、控制面、供应商、数据库、Redis、事件总线和分析系统。
  - 验收：每条跨边界数据流标记认证、加密和敏感级别。
  - 完成：2026-07-30
  - 证据：`docs/architecture/system-context.md`，含 TB-1～TB-5 与 DF-1～DF-7。

- [x] **P01-T03 编写威胁模型**
  - 产物：`docs/security/threat-model.md`。
  - 覆盖：Key 泄漏、越权、SSRF、Prompt 泄漏、日志泄漏、预算绕过、重放、DoS、供应链和恶意 Provider 响应。
  - 验收：每个高风险威胁有缓解措施、测试和负责人。
  - 完成：2026-07-30
  - 证据：`docs/security/threat-model.md`，覆盖 15 项 STRIDE 风险和零容忍事件。

- [x] **P01-T04 定义服务等级目标 SLO**
  - 产物：`docs/architecture/slo.md`。
  - 指标：可用性、网关附加延迟、首 Token 附加延迟、取消传播时间、计量完整率、配置同步延迟。
  - 验收：每个 SLO 有测量方式和误差预算，不使用无法验证的宣传数字。
  - 完成：2026-07-30
  - 证据：`docs/architecture/slo.md`。

- [x] **P01-T05 定义错误与重试语义**
  - 产物：ADR：首包前/首包后失败、可重试错误、客户端取消、超时层级和部分成功。
  - 验收：明确首包后默认不跨模型拼接；错误响应包含 requestId 与 retryable。
  - 完成：2026-07-30
  - 证据：`docs/api/error-and-retry-semantics.md`、ADR 0005。

- [x] **P01-T06 定义核心领域模型**
  - 产物：领域图和术语表。
  - 实体：Tenant、Project、VirtualKey、LogicalModel、Deployment、RoutePolicy、GatewayRequest、RouteAttempt、UsageLedger、BudgetReservation。
  - 验收：不存在“模型”“请求”“消费”等歧义术语。
  - 完成：2026-07-30
  - 证据：`docs/architecture/domain-model.md`，含聚合、不变量和状态机。

- [x] **P01-T07 设计数据库 ER 图**
  - 产物：`docs/architecture/er-diagram.md`。
  - 验收：主键、租户边界、唯一约束、版本、审计字段、索引和保留策略完整。
  - 完成：2026-07-30
  - 证据：`docs/architecture/er-diagram.md`。

- [x] **P01-T08 设计 Provider Adapter 契约**
  - 产物：接口、能力模型、规范化请求/响应/流事件/错误/Usage Schema。
  - 验收：契约可以表达缓存读写 Token、推理 Token、工具调用和未知计量字段。
  - 完成：2026-07-30
  - 证据：`docs/api/provider-adapter-contract.md`。

- [x] **P01-T09 设计公开 API**
  - 产物：OpenAPI 草案。
  - 覆盖：`/v1/chat/completions`、`/v1/models`、请求查询和管理面基础 API。
  - 验收：普通与 SSE 响应、错误码、Header 和兼容性边界写清楚。
  - 完成：2026-07-30
  - 证据：`api/openapi.yaml` 已通过 PyYAML 解析，包含 5 个 Path 和 13 个 Schema。

- [x] **P01-T10 编写首批 ADR**
  - 至少包含：Go/HTTP 选型、三进程架构、配置事实源、Outbox/事件、不可变 Usage Ledger、首包后不切换、内容默认不落盘。
  - 验收：每份 ADR 包含背景、决策、替代方案和后果。
  - 完成：2026-07-30
  - 证据：ADR 0001～0006，覆盖决策治理、Go/三进程、配置快照、Outbox/Ledger、首包切换和内容留存。

### P01 阶段门禁

- [x] MVP 边界和非目标明确。
- [x] API、数据模型、失败语义和威胁模型完成评审。
- [x] 所有 P0 能力都能映射到领域模型和验收测试。

---

## P02 本地基础设施与工程工具

目标：建立可重复、无真实密钥的本地开发环境和自动化入口。

- [x] **P02-T01 建立目录骨架**
  - 产物：`cmd`、`internal`、`api`、`migrations`、`deploy`、`tests`、`docs`。
  - 验收：模块依赖方向写入目录 README。
  - 完成：2026-07-30
  - 证据：各目录及 README 已建立，`internal/README.md` 记录依赖方向。

- [x] **P02-T02 初始化 Go Module 与基础依赖**
  - 产物：`go.mod`、`go.sum`。
  - 验收：依赖最小化，记录许可证；没有引入功能重复的大型框架。
  - 完成：2026-07-30
  - 证据：Go 1.26.5；`go.mod` 使用真实仓库模块 `github.com/zse04152005-del/ai-gateway-platform`/Go 1.26.5（远程 CI 首次运行发现 Go 1.26.0 标准库漏洞后提升至已修复版本）；初始 `go mod tidy` 与 `go list -m -json` 成功。P02-T06 首次引入并固定 golang-migrate v4.19.1/lib/pq v1.10.9，已生成 `go.sum`，用途和 MIT 许可证登记见 `docs/development/dependencies.md`。

- [x] **P02-T03 创建 Docker Compose 基础设施**
  - 组件：PostgreSQL、Redis、Redpanda、ClickHouse、Prometheus、Grafana、OpenTelemetry Collector。
  - 验收：均有健康检查、固定开发版本和持久化卷说明。
  - 完成：2026-07-30
  - 证据：`docker compose config --quiet`、7 个固定版本镜像拉取和 `up -d --wait` 均真实通过；PostgreSQL SQL、Redis PING、Redpanda 集群健康、ClickHouse 查询、Prometheus、Grafana、OTel 健康端点与 Prometheus→OTel Target 均通过。发现并修复 ClickHouse 探针将 `localhost` 解析到未监听的 IPv6 `::1` 问题；6 个命名卷用途和安全清理说明见 `deploy/compose/README.md`。运行环境为 Docker Desktop 4.84.0、Engine 29.6.2、Compose v5.3.1、WSL 2.7.11.0/内核 6.18.33.2。

- [x] **P02-T04 建立统一任务入口**
  - 产物：Makefile/Taskfile。
  - 命令：bootstrap、lint、test、test-integration、migrate、compose-up、compose-down、run、load-test。
  - 验收：README 中不用复制长命令即可启动环境。
  - 完成：2026-07-30
  - 证据：`Makefile`、`scripts/dev.ps1`、README 本地开发入口；PowerShell bootstrap/test 均真实执行成功，并显式检查外部命令退出码。

- [x] **P02-T05 建立配置模板**
  - 产物：`.env.example`、配置 Schema 和校验器。
  - 验收：示例不含真实 Key；缺失必需配置时启动失败并给出明确错误。
  - 完成：2026-07-30
  - 证据：`.env.example`、`internal/config`；`go vet` 与单元测试通过，覆盖率 83.0%，验证缺失数据库、非法地址、环境、日志级别、时长和生产本地密钥。

- [x] **P02-T06 建立数据库迁移工具**
  - 产物：迁移命令、空白基线和回滚约定。
  - 验收：新库可一键迁移；重复执行安全；CI 能检测迁移顺序。
  - 完成：2026-07-30
  - 证据：新增项目内 `cmd/migrate` 的 validate/up/down/version 与 `internal/migration` 顺序校验；单测覆盖配对、缺号、冲突名、非法名、空 SQL、嵌套目录和 Windows file URL。唯一临时 PostgreSQL 库真实通过空库 up、重复 up/no-change、version、生产 down 拒绝、开发 down、再次 up，最终 `schema_count:version:dirty=2:1:false`，临时库已删除；PowerShell 一键入口也已在主开发库通过。

- [x] **P02-T07 配置格式化、Lint 和静态检查**
  - 工具：gofmt、go vet、golangci-lint、govulncheck 等。
  - 验收：本地和 CI 使用同一配置。
  - 完成：2026-07-30
  - 证据：`go.mod` tool 指令固定 golangci-lint v2.12.2、govulncheck v1.6.0；`.golangci.yml` 已迁移到 v2 并显式固定 linter/formatter 集。本地统一 `check` 真实通过 go mod verify、格式门禁、vet、golangci-lint `0 issues`、单测、构建、govulncheck `No vulnerabilities found`、迁移校验；CI 调用相同 `go tool` 与配置。

- [x] **P02-T08 建立 CI 基线**
  - 内容：格式、Lint、单测、依赖漏洞、构建和密钥扫描。
  - 验收：故意制造格式错误和测试失败时 CI 能阻止合并。
  - 完成：2026-07-30
  - 证据：`.github/workflows/ci.yml` 包含 Go 质量/构建、PostgreSQL 迁移状态机、配置与 Gitleaks 三个 Job；actionlint v1.7.12 通过，10 个 YAML 文件静态解析通过。临时加入未格式化且失败的 Go 测试后，格式门禁与 `go test` 均以退出码 1 拒绝，随后临时文件已删除并重跑全套正向门禁通过。首次远程运行 `30524635838` 真实拦截 Go 1.26.0 标准库漏洞与 YAML 末尾空行；修复为 Go 1.26.5、规范 YAML 并升级官方 Actions 后，运行 `30525356663` 全绿。真实仓库模块路径对齐后的最终运行 `30525934377` 再次全绿。

- [x] **P02-T09 建立测试夹具目录**
  - 产物：Provider JSON/SSE、错误、Usage 和缓存 Token Fixture。
  - 验收：Fixture 不含第三方真实响应中的敏感数据。
  - 完成：2026-07-30
  - 证据：`tests/fixtures/providers` 场景规划及人工构造的 JSON/SSE 成功样本；JSON 静态解析通过。

### P02 阶段门禁

- [x] 新环境可根据 README 启动全部依赖（2026-07-30 在新安装 Docker/WSL 环境真实通过）。
- [x] CI 基线通过（2026-07-30：本地等价门禁与负向测试通过；远程 GitHub Actions 运行 [`30525934377`](https://github.com/zse04152005-del/ai-gateway-platform/actions/runs/30525934377) 的三个 Job 全绿）。
- [x] 配置与密钥不会被误提交（`.env` 被 Git 忽略，本地高风险模式扫描、远程模式扫描与 Gitleaks 均通过）。

---

## P03 应用骨架与横切能力

目标：建立三个可运行进程和统一的配置、日志、错误、生命周期管理。

- [x] **P03-T01 创建 `gateway` 进程**
  - 验收：提供 `/health/live`、`/health/ready`，支持优雅关闭。
  - 完成：2026-07-30
  - 证据：提交 `fb46bb7` 新增 `cmd/gateway` 与初始 HTTP 生命周期实现，P03-T02 将通用部分无行为变化地提升为 `internal/httpserver`；live 始终反映进程存活，ready 与监听/关闭生命周期绑定，非 GET 返回 405，未就绪返回 503/`Retry-After`；关闭先撤销 readiness，再在 `SHUTDOWN_TIMEOUT` 内排空普通在途请求，超时强制关闭并返回错误。初始定向测试连续 20 轮通过，核心/命令层覆盖率分别为 83.8%/61.5%，完整本地门禁通过；远程 GitHub Actions [`30527478935`](https://github.com/zse04152005-del/ai-gateway-platform/actions/runs/30527478935) 的 Linux race、构建、漏洞、密钥与迁移 Job 全绿。

- [x] **P03-T02 创建 `control-plane` 进程**
  - 验收：健康检查与基础管理路由可用。
  - 完成：2026-07-30
  - 证据：提交 `3d92e83` 新增独立 `cmd/control-plane`、`internal/controlplane` 的 `GET /admin/v1/status`，并将 gateway 已验证的健康/readiness/有限时关闭能力无行为变化地抽取为共享 `internal/httpserver`。四个相关包连续 20 轮通过；共享 HTTP 层、control-plane 路由层和命令层覆盖率分别为 88.7%、83.3%、61.5%，完整本地门禁通过；远程 GitHub Actions [`30528927423`](https://github.com/zse04152005-del/ai-gateway-platform/actions/runs/30528927423) 的 Linux race、构建、漏洞、密钥与迁移 Job 全绿。

- [x] **P03-T03 创建 `metering-worker` 进程**
  - 验收：可连接事件总线、响应停止信号并优雅退出。
  - 完成：2026-07-30
  - 证据：提交 `9592085` 新增 `cmd/metering-worker` 与 `internal/meteringworker`，实现 broker 地址强校验、按序回退、启动/关闭超时、原子连接状态、取消启动、会话关闭和错误传播；集成标签测试真实连接本地 Redpanda `localhost:19092`。worker 核心/命令层覆盖率为 87.5%/55.6%，20 轮重复测试与完整本地门禁通过；远程 GitHub Actions [`30529910183`](https://github.com/zse04152005-del/ai-gateway-platform/actions/runs/30529910183) 的 Linux race、三进程构建、漏洞、密钥与迁移 Job 全绿。

- [~] **P03-T04 实现配置加载与热配置快照接口**
  - 验收：环境配置启动时强校验；业务配置使用不可变 Snapshot。

- [ ] **P03-T05 实现结构化日志**
  - 字段：service、version、requestId、traceId、tenantId、projectId、level。
  - 验收：默认脱敏 Authorization、Cookie、Prompt、Response 和 Provider Key。

- [ ] **P03-T06 实现统一错误模型**
  - 验收：内部错误与外部错误分离；生产响应不暴露栈和内部地址。

- [ ] **P03-T07 实现请求 ID 与 Trace Context 中间件**
  - 验收：客户端 requestId 冲突/非法时由服务端重新生成；跨进程可关联。

- [ ] **P03-T08 实现优雅关闭与连接管理**
  - 验收：停止接收新请求，等待有限时间完成普通请求，取消剩余流式请求。

- [ ] **P03-T09 建立基础单测与集成测试模板**
  - 验收：三个进程都有启动、健康、取消和配置错误测试。

### P03 阶段门禁

- [ ] 三个进程可以独立启动与停止。
- [ ] 日志、错误、Request ID 和配置行为一致。

---

## P04 租户、项目、虚拟 Key 与模型目录

目标：完成数据面的身份来源和控制面的最小资源管理。

- [ ] **P04-T01 创建 Tenant/Project 数据库迁移**
  - 验收：状态、配额引用、审计时间和租户唯一约束完整。

- [ ] **P04-T02 创建 Virtual API Key 数据模型**
  - 字段：prefix、secret_hash、project、status、expiresAt、allowModels、limits。
  - 验收：数据库不保存可恢复的虚拟 Key 明文。

- [ ] **P04-T03 实现 Key 创建、轮换、禁用和过期**
  - 验收：明文只返回一次；旧 Key 的过渡策略有测试。

- [ ] **P04-T04 实现数据面 Key 认证中间件**
  - 验收：禁用、过期、错误、越权和缓存失效测试通过。

- [ ] **P04-T05 创建 Provider/LogicalModel/Deployment 模型**
  - 验收：逻辑模型与物理部署分离；能力和区域可表达。

- [ ] **P04-T06 实现模型白名单与 `/v1/models`**
  - 验收：只返回当前 Key/项目允许且可用的逻辑模型。

- [ ] **P04-T07 实现供应商密钥引用**
  - MVP 可用本地开发加密器，接口预留 Vault/KMS。
  - 验收：日志、错误和 API 不能返回明文。

- [ ] **P04-T08 实现租户隔离测试**
  - 验收：直接 ID、列表、缓存、Key 和模型查询均不能跨租户。

### P04 阶段门禁

- [ ] 虚拟 Key 生命周期完整。
- [ ] 模型目录与能力契约可用。
- [ ] 多租户越权测试通过。

---

## P05 Mock Provider 与适配器一致性框架

目标：在接真实供应商前建立可控、确定性的协议测试环境。

- [ ] **P05-T01 实现 Mock Provider 服务**
  - 能力：普通响应、SSE、固定 Token、延迟、429、5xx、断流、错误 Chunk、缓存 Token 和工具调用。
  - 验收：每种行为可通过请求参数或测试场景 ID 精确触发。

- [ ] **P05-T02 定义 NormalizedRequest/Response/Chunk/Error/Usage**
  - 验收：类型不依赖特定供应商；未知 Usage 字段可保留原始证据。

- [ ] **P05-T03 实现 Provider Adapter 注册表**
  - 验收：按类型加载适配器；未知类型启动/发布失败。

- [ ] **P05-T04 实现 Mock Adapter**
  - 验收：普通、流式、错误和 usage Fixture 全部通过。

- [ ] **P05-T05 建立 Adapter Conformance Suite**
  - 测试：普通、流式、取消、错误、缓存 Token、工具调用、finish reason 和未知字段。
  - 验收：新增适配器只需注册 Fixture 即可运行统一测试。

- [ ] **P05-T06 建立协议金丝雀测试**
  - 目标：定期对真实供应商最小请求，发现字段漂移。
  - MVP：先实现接口和 Mock。

- [ ] **P05-T07 实现一个真实 Provider Adapter**
  - 优先使用用户后续提供且允许使用的供应商，不在仓库保存 Key。
  - 验收：通过一致性套件中的适用测试，并记录不支持项。

### P05 阶段门禁

- [ ] 所有数据面开发都可以不依赖外网运行。
- [ ] 适配器行为由自动化 Fixture 验证。

---

## P06 非流式数据面核心链路

目标：完成一次可认证、可路由、可代理、可追踪的非流式 Chat 请求。

- [ ] **P06-T01 实现 `/v1/chat/completions` 参数解析**
  - 验收：请求大小、模型、messages、参数类型和不支持字段有明确错误。

- [ ] **P06-T02 实现请求规范化**
  - 验收：客户端协议转换为 NormalizedRequest，保留 requestId 和幂等信息。

- [ ] **P06-T03 实现 Deployment 选择最小版本**
  - MVP：固定路由或优先级第一健康实例。
  - 验收：无候选时返回统一错误。

- [ ] **P06-T04 实现上游 HTTP Client**
  - 配置：连接、TLS、Header、总超时、响应头超时、连接池。
  - 验收：不会为每个请求新建 Client；敏感 Header 不被透传。

- [ ] **P06-T05 实现普通响应解析与统一输出**
  - 验收：usage、finish reason、tool calls 和错误映射正确。

- [ ] **P06-T06 建立 GatewayRequest/RouteAttempt 记录**
  - 验收：每次上游尝试具有独立 Attempt，状态迁移合法。

- [ ] **P06-T07 实现请求取消传播**
  - 验收：客户端取消能取消上游 Context，并记录 `client_cancelled`。

- [ ] **P06-T08 完成非流式端到端测试**
  - 覆盖：成功、认证失败、模型不可用、超时、429、5xx 和客户端取消。

### P06 阶段门禁

- [ ] 非流式 Chat 全链路可用。
- [ ] 所有请求都有 Request/Attempt/Trace 关联。

---

## P07 SSE 流式代理与部分失败语义

目标：实现资源可控、取消可靠、用量不丢失的流式核心。

- [ ] **P07-T01 实现 SSE 上游解析器**
  - 验收：处理分块边界、空行、`[DONE]`、多行 data、非法事件和超大 Chunk。

- [ ] **P07-T02 实现 SSE 下游写入器**
  - 验收：正确 Header、及时 Flush、写超时和客户端断开检测。

- [ ] **P07-T03 实现有界缓冲与背压**
  - 验收：慢客户端不会造成无限内存增长；达到上限后按策略取消。

- [ ] **P07-T04 实现首 Token 与 no-progress timeout**
  - 验收：区分连接超时、首 Token 超时、流中停顿和总时限。

- [ ] **P07-T05 实现可选 SSE heartbeat**
  - 验收：不会改变模型内容语义；客户端可配置关闭。

- [ ] **P07-T06 实现客户端取消与上游释放验证**
  - 验收：记录取消传播时间；上游连接和 Goroutine 不泄漏。

- [ ] **P07-T07 实现部分失败 Attempt**
  - 验收：已输出部分内容后失败，Attempt 标记 partial，保留已知 Usage 和结束原因。

- [ ] **P07-T08 禁止首包后透明跨模型拼接**
  - 验收：自动化测试证明首包后不会启动备用模型并继续同一响应。

- [ ] **P07-T09 实现流式 Usage 汇总**
  - 验收：供应商最终 Usage、逐 Chunk Usage 和本地估算三种来源可区分。

- [ ] **P07-T10 完成流式压力与泄漏测试**
  - 覆盖：大量连接、慢客户端、随机取消、上游断流、长 TTFT。

### P07 阶段门禁

- [ ] 普通与异常 SSE Fixture 全部通过。
- [ ] 客户端取消后资源可在 SLO 时间内释放。
- [ ] 部分失败不会丢失 Attempt 和 Usage。

---

## P08 路由、健康、重试、熔断与降级

目标：实现可解释、不会放大故障和费用的多部署治理。

- [ ] **P08-T01 实现候选过滤器**
  - 顺序：租户白名单、能力、区域、状态、健康、预算和容量。
  - 验收：每个过滤原因可记录和查询。

- [ ] **P08-T02 实现固定、优先级和加权路由**
  - 验收：随机种子可注入，测试可重复。

- [ ] **P08-T03 实现被动健康统计**
  - 指标：请求量、429、5xx、超时、首 Token、总延迟。
  - 验收：低样本不误触发异常状态。

- [ ] **P08-T04 实现主动健康检查**
  - 验收：健康请求低频、低成本且与生产调用隔离。

- [ ] **P08-T05 实现熔断状态机**
  - 验收：Closed/Open/Half-Open 状态和并发探测限制正确。

- [ ] **P08-T06 定义并实现重试分类**
  - 验收：认证、参数、内容拒绝不重试；429/部分 5xx 受总时间预算限制。

- [ ] **P08-T07 实现首包前故障切换**
  - 验收：每次切换生成新 Attempt；总费用包含全部 Attempt。

- [ ] **P08-T08 实现路由决策解释记录**
  - 记录：候选、过滤原因、评分、策略版本和最终选择。
  - 验收：可通过 requestId 复盘。

- [ ] **P08-T09 测试故障风暴下的请求放大**
  - 验收：最大 Attempt 和总时间受控，不产生指数级重试。

### P08 阶段门禁

- [ ] 多部署故障切换可用。
- [ ] 路由决策可解释。
- [ ] 重试不会绕过预算和计量。

---

## P09 多级限流与预算预留

目标：实现租户、项目、Key 等维度的容量和费用硬约束。

- [ ] **P09-T01 定义 RPM/TPM/并发策略 Schema**
  - 验收：支持继承、覆盖、软阈值和硬阈值。

- [ ] **P09-T02 实现本地快速限流**
  - 验收：保护单实例，配置变化可热更新。

- [ ] **P09-T03 实现 Redis 原子 RPM 限流**
  - 验收：并发测试下计数准确，TTL 和时钟边界明确。

- [ ] **P09-T04 实现 TPM 预估与结算**
  - 验收：输入与最大输出先占用，完成后按实际释放差额。

- [ ] **P09-T05 实现分布式并发限制**
  - 验收：正常结束、失败、取消和进程退出后槽位可释放/过期。

- [ ] **P09-T06 创建预算账本与预留表**
  - 验收：项目、Key、用户/会话和租户维度可以独立建账。

- [ ] **P09-T07 实现原子预算预留**
  - 验收：100+ 并发请求不能突破硬预算；版本冲突可重试且有上限。

- [ ] **P09-T08 实现预算结算与差额释放**
  - 验收：多 Attempt、缓存命中、取消和失败分别正确处理。

- [ ] **P09-T09 实现过期预留 Reaper**
  - 验收：异常退出留下的预留可回收，回收生成审计/对账事件。

- [ ] **P09-T10 实现软预算告警与硬预算错误**
  - 验收：错误包含剩余额度、重置时间和可选降级提示，不泄露他人数据。

### P09 阶段门禁

- [ ] 限流维度和错误语义统一。
- [ ] 并发预算测试不超卖。
- [ ] 预留没有永久泄漏。

---

## P10 用量账本、价格版本与幂等计量

目标：任何请求路径都能形成可追溯、可修正、不会重复的费用事实。

- [ ] **P10-T01 创建 Request/Attempt/Usage Ledger 迁移**
  - 验收：Usage 追加写入；具有 eventId 和唯一约束。

- [ ] **P10-T02 定义 Token 类型与计量来源**
  - 类型：input、output、cache_read、cache_write、reasoning、audio/image 等。
  - 来源：provider、estimated、reconciled、adjustment。

- [ ] **P10-T03 创建价格版本模型**
  - 验收：生效时间、区域、币种、单位和 Token 类型完整；历史请求锁定版本。

- [ ] **P10-T04 实现用量事件发布**
  - 验收：网关热路径不等待 ClickHouse；事件失败有 Outbox/受限缓冲策略。

- [ ] **P10-T05 实现 Metering 幂等消费**
  - 验收：同一 eventId 重放 10 次只生成一份有效分录。

- [ ] **P10-T06 实现多 Attempt 成本聚合**
  - 验收：失败 Attempt、部分流和最终成功 Attempt 的费用都被计入。

- [ ] **P10-T07 实现本地 Token 估算接口**
  - 验收：明确 tokenizer/model 版本和 estimated 标记；不能冒充供应商精确值。

- [ ] **P10-T08 实现 Adjustment 分录**
  - 验收：修正不覆盖原记录；可以解释修正来源和操作者。

- [ ] **P10-T09 实现费用查询 API**
  - 验收：按 requestId 查看每个 Attempt、Token 类型、价格版本与金额。

- [ ] **P10-T10 构造缓存/流式/失败计费回归集**
  - 用公开问题线索构造自己的 Fixture。
  - 验收：缓存 Token 丢失、流式 usage 缺失、回退漏记均有测试。

### P10 阶段门禁

- [ ] 任何 Attempt 都有明确计量状态。
- [ ] 重复事件不重复计费。
- [ ] 估算与供应商 Usage 能区分。

---

## P11 控制面、配置版本与数据面分发

目标：完成可发布、可回滚、控制面故障不拖垮数据面的配置链路。

- [ ] **P11-T01 实现租户、项目、Key 管理 API**
  - 验收：权限、分页、审计和敏感字段隐藏完整。

- [ ] **P11-T02 实现 Provider/Model/Deployment 管理 API**
  - 验收：能力、密钥引用、区域、状态和价格验证完整。

- [ ] **P11-T03 实现 Route Policy 草稿与发布版本**
  - 验收：发布版本不可变，草稿不影响生产流量。

- [ ] **P11-T04 实现配置静态校验**
  - 验收：无可用部署、循环回退、未知能力、缺失价格和无效密钥引用不能发布。

- [ ] **P11-T05 实现配置快照构建**
  - 验收：包含版本、校验和、生成时间和必要数据，不包含多余敏感信息。

- [ ] **P11-T06 实现配置分发**
  - 可使用 Redis Pub/Sub/Stream 或 Kafka 通知，数据面按版本拉取。
  - 验收：丢通知后仍可通过周期校验恢复。

- [ ] **P11-T07 实现最近有效配置本地缓存**
  - 验收：控制面/Redis 短时不可用时继续服务；过久未更新触发告警。

- [ ] **P11-T08 实现发布回滚**
  - 验收：可回滚到已知版本，并保留操作原因和审计。

- [ ] **P11-T09 实现配置版本 Dashboard/接口**
  - 验收：能发现实例配置落后和校验和不一致。

### P11 阶段门禁

- [ ] 草稿、发布和回滚闭环完整。
- [ ] 数据面不在热路径查询控制面数据库。
- [ ] 控制面故障演练通过。

---

## P12 安全与隐私治理

目标：确保密钥、租户数据和 Prompt 默认受到保护。

- [ ] **P12-T01 实现管理面身份与 RBAC**
  - 角色：平台管理员、租户管理员、项目负责人、审计员。
  - 验收：每个管理 API 有权限测试。

- [ ] **P12-T02 实现租户上下文强制注入**
  - 验收：业务 Repository 不接受客户端自由指定 tenantId。

- [ ] **P12-T03 实现供应商密钥信封加密接口**
  - 验收：开发/生产实现可替换；轮换与 key version 可追踪。

- [ ] **P12-T04 实现统一敏感字段脱敏器**
  - 覆盖：日志、Trace、错误、审计、管理 API 和导出。
  - 验收：自动化测试搜索明文 Key/Prompt 不应出现。

- [ ] **P12-T05 实现内容留存策略**
  - 默认只存长度、哈希和分类；明文采样需显式启用、期限和访问权限。
  - 验收：不同租户策略隔离。

- [ ] **P12-T06 实现 Provider Endpoint SSRF 防护**
  - 验收：阻止回环、元数据、保留地址、DNS 重绑定和未允许域名。

- [ ] **P12-T07 实现请求大小、Header 和流 Chunk 限制**
  - 验收：超大输入、压缩炸弹和异常 Chunk 不导致内存失控。

- [ ] **P12-T08 实现审计日志**
  - 覆盖：Key、模型、部署、价格、路由、预算、内容策略和导出。
  - 验收：记录 before/after 摘要、actor、reason、IP 和 traceId。

- [ ] **P12-T09 完成安全测试基线**
  - 覆盖：Key 猜测、越权、SSRF、日志注入、Header 注入、缓存污染和 DoS。

### P12 阶段门禁

- [ ] Prompt/Response/Key 默认不泄漏。
- [ ] 多租户和管理权限测试通过。
- [ ] 高风险威胁均有测试证据。

---

## P13 可观测性、运维与 MVP 门禁

目标：让每次请求、路由、Attempt、计量和故障都可观察、可定位。

- [ ] **P13-T01 接入 OpenTelemetry SDK**
  - 验收：gateway、control-plane、worker Trace 可关联。

- [ ] **P13-T02 设计 Span 层级**
  - 层级：request、auth、policy、route、attempt、provider、usage_publish。
  - 验收：Span 不含正文和密钥。

- [ ] **P13-T03 实现核心 Prometheus 指标**
  - 覆盖：请求、延迟、TTFT、活跃流、Token、成本、回退、熔断、限流、预算和队列积压。

- [ ] **P13-T04 控制指标基数**
  - 验收：不得把 requestId、userId、Prompt 或无限模型名作为 Metric Label。

- [ ] **P13-T05 创建 Grafana Dashboard**
  - 页面：总览、Provider、流式连接、成本/预算、Metering、控制面配置。

- [ ] **P13-T06 创建告警规则**
  - 覆盖：错误率、TTFT、回退激增、计量积压、预算异常、配置落后和审计异常。

- [ ] **P13-T07 编写 Runbook**
  - 场景：Provider 429、流式泄漏、预算锁死、用量积压、配置发布失败和 ClickHouse 故障。

- [ ] **P13-T08 运行 P0 端到端回归**
  - 验收：P04～P12 关键用例全部通过。

- [ ] **P13-T09 执行 MVP 故障演练**
  - 场景：控制面停止、Redis 短暂失败、事件总线失败、Provider 断流、Worker 重启。

- [ ] **P13-T10 形成 MVP 验收报告**
  - 产物：`docs/reports/mvp-acceptance.md`。
  - 验收：未满足项明确列出，不能用“基本完成”代替。

### P13 MVP 阶段门禁

- [ ] 普通/流式代理、认证、路由、限流、预算和计量完整。
- [ ] 流式部分失败、取消和多 Attempt 计费正确。
- [ ] 控制面故障不影响最近有效配置。
- [ ] 安全、遥测和 Runbook 齐全。
- [ ] MVP 验收报告通过后，才能进入 P1。

---

## P14 精确缓存与安全失效（P1）

- [ ] **P14-T01 定义缓存适用策略**
  - 默认排除：工具调用、高 temperature、高风险、时间敏感和禁止留存请求。

- [ ] **P14-T02 实现规范化 Cache Key**
  - 必含：tenant、model family、messages hash、参数、template/policy/knowledge version。

- [ ] **P14-T03 实现租户隔离的 Redis 缓存**
  - 验收：跨租户永不命中，TTL 和大小受控。

- [ ] **P14-T04 记录缓存来源与 Usage**
  - 验收：区分网关缓存与供应商 Prompt Cache，不重复计费。

- [ ] **P14-T05 实现失效与版本升级测试**
  - 验收：模板、策略、知识或权限版本变化后旧结果不返回。

- [ ] **P14-T06 测量缓存收益**
  - 输出：命中率、延迟节省、供应商成本节省和 Redis 成本。

---

## P15 分析报表与供应商账单对账（P1）

- [ ] **P15-T01 设计 ClickHouse 明细和聚合表**
  - 验收：分区、排序键、TTL 和物化视图有数据量依据。

- [ ] **P15-T02 实现用量写入 ClickHouse**
  - 验收：重复事件幂等或可去重；故障后可补写。

- [ ] **P15-T03 实现成本/延迟/错误报表 API**
  - 维度：日期、租户、项目、模型、部署、状态和缓存。

- [ ] **P15-T04 定义供应商账单导入格式**
  - 验收：文件哈希、批次、币种、时区和来源可追踪。

- [ ] **P15-T05 实现账单导入与幂等**
  - 验收：重复导入同一批次不会重复生成差异。

- [ ] **P15-T06 实现对账匹配策略**
  - 优先 provider request reference，其次时间/模型/Token 聚合匹配。

- [ ] **P15-T07 实现五类差异检测**
  - 漏记、重复、Token 偏差、价格偏差、未知费用。

- [ ] **P15-T08 创建对账报告与告警**
  - 验收：差异率超过门槛时通知；可追溯到网关 Request/Attempt。

---

## P16 质量评测、Shadow 与可解释智能路由（P1）

- [ ] **P16-T01 定义用例级评测数据集格式**
  - 包含版本、权限、脱敏、预期结构和评分器。

- [ ] **P16-T02 实现离线评测 Runner**
  - 验收：可比较多个 Deployment 的质量、延迟和成本。

- [ ] **P16-T03 实现模型质量门禁**
  - 验收：未达到阈值的 Deployment 不能加入生产策略。

- [ ] **P16-T04 实现 Shadow 请求记录**
  - 原则：不影响主响应；受隐私和额外费用策略控制。

- [ ] **P16-T05 实现评分路由**
  - 质量门槛先过滤，再根据延迟、健康和成本评分。

- [ ] **P16-T06 实现路由决策查询**
  - 验收：可查看候选、过滤、分数、策略和评测版本。

- [ ] **P16-T07 防止在线反馈直接污染权重**
  - 验收：反馈只进入评测/审批流程，不能即时自动改生产策略。

---

## P17 模型生命周期、策略灰度与数据驻留（P1）

- [ ] **P17-T01 定义 Deployment 生命周期**
  - 状态：draft、evaluation、shadow、canary、production、deprecated、retired。

- [ ] **P17-T02 实现版本固定与别名解析**
  - 验收：逻辑模型可固定物理版本，避免供应商别名漂移。

- [ ] **P17-T03 实现依赖扫描**
  - 验收：下线模型前列出受影响项目、Key 和策略。

- [ ] **P17-T04 实现策略灰度发布**
  - 验收：按项目/Key/百分比灰度并可快速回滚。

- [ ] **P17-T05 实现区域与数据驻留策略**
  - 验收：受限请求不能路由到不允许区域；决策可审计。

- [ ] **P17-T06 建立模型弃用通知流程**
  - 验收：提前告警、迁移建议和截止日期可追踪。

---

## P18 全面测试、性能优化与发布（P1）

- [ ] **P18-T01 建立 k6/Vegeta 压测脚本**
  - 场景：非流式、SSE、慢客户端、取消、重试和混合流量。

- [ ] **P18-T02 建立性能基线**
  - 输出：吞吐、P50/P95/P99、TTFT、连接数、CPU、内存、网络和 GC。

- [ ] **P18-T03 运行并发预算与限流压力测试**
  - 验收：硬预算不超卖，限流误差在设计范围内。

- [ ] **P18-T04 运行重试/熔断故障风暴测试**
  - 验收：请求放大和供应商压力受控。

- [ ] **P18-T05 运行长连接稳定性测试**
  - 验收：持续时间内无明显 Goroutine、内存和 FD 泄漏。

- [ ] **P18-T06 运行安全回归**
  - 覆盖：越权、SSRF、密钥、日志、请求大小、依赖漏洞和镜像扫描。

- [ ] **P18-T07 创建 Docker 生产镜像**
  - 验收：非 root、最小镜像、健康检查、SBOM 和版本标签。

- [ ] **P18-T08 创建 Kubernetes/Helm 配置**
  - 验收：资源限制、探针、PDB、NetworkPolicy、HPA 和 Secret 引用完整。

- [ ] **P18-T09 实现灰度发布与回滚演练**
  - 验收：代码、配置和数据库迁移均有回滚路径。

- [ ] **P18-T10 输出 P1 发布报告**
  - 产物：性能、安全、故障、对账和已知限制报告。

---

## P19 MCP/A2A 工具治理（P2）

前置条件：P18 完成，且核心模型网关稳定。

- [ ] **P19-T01 定义 MCP Server/Tool 资产模型**
  - 字段：Owner、Schema、版本、风险、认证、网络、数据级别。

- [ ] **P19-T02 实现 MCP Server 注册与健康检查**
  - 验收：支持有限的 HTTP/Streamable HTTP；stdio 作为隔离扩展评估。

- [ ] **P19-T03 实现工具级 RBAC/ABAC**
  - 权限精确到 server/tool/resource/action。

- [ ] **P19-T04 实现 OAuth/凭证引用**
  - 验收：Agent 无法获得下游长期明文凭证。

- [ ] **P19-T05 实现工具风险等级与人工审批**
  - 高风险 Tool Call 需要一次性批准令牌，绑定参数摘要和过期时间。

- [ ] **P19-T06 实现 Tool Call 幂等与审计**
  - 验收：重试不重复执行有副作用操作。

- [ ] **P19-T07 实现 Agent 执行预算**
  - 限制：最大步骤、模型 Token、工具次数、总费用和总时长。

- [ ] **P19-T08 实现循环检测与熔断**
  - 验收：Agent-A2A/Tool 循环不会无限放大。

- [ ] **P19-T09 完成工具注入与越权安全测试**
  - 验收：恶意 Tool 描述不能改变网关策略或读取未授权数据。

---

## P20 自托管模型推理感知路由（P2）

前置条件：有可用 vLLM/Kubernetes 测试环境，否则只实现契约和模拟器。

- [ ] **P20-T01 定义 Inference Endpoint Picker 接口**
  - 输入：模型、估算 Token、优先级、会话/前缀摘要和能力。
  - 输出：Endpoint、理由、分数和租约信息。

- [ ] **P20-T02 建立推理指标适配器**
  - 指标：queue depth、active requests、GPU memory、KV cache、LoRA availability。

- [ ] **P20-T03 实现请求成本估算**
  - 验收：输入长度、最大输出和多模态大小进入调度成本。

- [ ] **P20-T04 实现 KV-cache/前缀亲和模拟路由**
  - 验收：在模拟工作负载中比较轮询的 TTFT 与命中率。

- [ ] **P20-T05 实现 LoRA Adapter 能力过滤**
  - 验收：请求只进入已加载或可加载所需 Adapter 的实例。

- [ ] **P20-T06 实现队列、公平性和优先级策略**
  - 验收：高流量租户不能饿死其他同等级租户。

- [ ] **P20-T07 对接 Kubernetes Inference Gateway/llm-d（可选）**
  - 验收：若环境不可用，明确标记为后续项，不伪造结果。

- [ ] **P20-T08 输出轮询与推理感知路由对比报告**
  - 指标：TTFT、P99、吞吐、KV Cache 命中和 GPU 利用率。

---

## P21 文档、演示、开源与简历交付

- [ ] **P21-T01 完善根 README 与快速开始**
  - 验收：新机器可在 30 分钟内运行 Mock 演示。

- [ ] **P21-T02 生成完整 API 与事件文档**
  - 验收：OpenAPI、错误码、SSE、事件 Schema 和兼容性边界完整。

- [ ] **P21-T03 完善架构、ER、时序图与 ADR**
  - 验收：图与实际代码一致，不保留过期设计。

- [ ] **P21-T04 编写运维 Runbook**
  - 覆盖：Provider 故障、预算异常、流式泄漏、计量积压、配置回滚和账单差异。

- [ ] **P21-T05 制作三个演示场景**
  - 场景 A：普通/流式多模型调用。
  - 场景 B：Provider 故障切换与路由解释。
  - 场景 C：并发预算、部分流失败和费用对账。

- [ ] **P21-T06 整理测试和性能证据**
  - 验收：所有简历数字都能追溯到脚本、环境和报告。

- [ ] **P21-T07 整理安全与隐私说明**
  - 验收：威胁、缓解、留存、密钥和租户隔离清晰。

- [ ] **P21-T08 建立贡献指南与 Issue 模板**
  - 产物：CONTRIBUTING、Bug/Feature/Security 模板。

- [ ] **P21-T09 检查第三方许可证与 NOTICE**
  - 验收：依赖和参考项目使用方式合规。

- [ ] **P21-T10 形成简历项目描述与面试题库**
  - 内容：背景、本人职责、难点、方案、权衡、真实指标和故障案例。

- [ ] **P21-T11 生成最终项目验收报告**
  - 验收：P0/P1/P2 完成情况、已知限制、遗留风险和下一步全部列出。

---

## 附录 A：任何阶段都不能遗漏的检查

### 每个写接口

- [ ] 是否需要幂等键？
- [ ] 是否校验租户与权限？
- [ ] 是否记录审计？
- [ ] 是否有并发更新策略？
- [ ] 错误是否隐藏内部信息？

### 每个异步事件

- [ ] 是否有 eventId、schemaVersion、occurredAt、tenantId 和 traceId？
- [ ] 是否至少一次投递？
- [ ] 消费者是否幂等？
- [ ] 是否有重试、死信和积压告警？
- [ ] Schema 升级是否向后兼容？

### 每个缓存

- [ ] Key 是否包含 tenant 和必要版本？
- [ ] 是否有 TTL、大小限制和失效机制？
- [ ] 缓存失败是否影响核心链路？
- [ ] 是否可能返回未授权或过期内容？

### 每个 Provider Adapter

- [ ] 普通、流式、取消、错误、缓存 Token 和工具调用测试是否通过？
- [ ] 未知字段是否可观察？
- [ ] 错误是否正确标记 retryable？
- [ ] usage 来源与估算状态是否准确？

### 每次发布

- [ ] 数据库迁移是否向后兼容？
- [ ] 配置是否可回滚？
- [ ] Dashboard 和告警是否更新？
- [ ] 安全、性能和故障回归是否通过？
- [ ] CHANGELOG、ADR 和执行清单是否更新？

## 附录 B：阶段完成记录

| 阶段 | 状态 | 完成日期 | 报告/证据 | 备注 |
|---|---|---|---|---|
| P00 | 已完成 | 2026-07-30 | Git 忽略检查、文档基线、技术栈和 DoD |  |
| P01 | 已完成 | 2026-07-30 | MVP、上下文、威胁、SLO、领域、ER、Adapter、OpenAPI、ADR |  |
| P02 | 已完成 | 2026-07-30 | `docs/reports/2026-07-30-p02-environment.md`、GitHub Actions `30525934377` | 新公开仓库已建立；本地与远程门禁全绿 |
| P03 | 进行中 |  |  | P03-T01～T03 已完成；当前主任务 P03-T04 |
| P04 | 未开始 |  |  |  |
| P05 | 未开始 |  |  |  |
| P06 | 未开始 |  |  |  |
| P07 | 未开始 |  |  |  |
| P08 | 未开始 |  |  |  |
| P09 | 未开始 |  |  |  |
| P10 | 未开始 |  |  |  |
| P11 | 未开始 |  |  |  |
| P12 | 未开始 |  |  |  |
| P13 | 未开始 |  |  | MVP 门禁 |
| P14 | 未开始 |  |  | P1 |
| P15 | 未开始 |  |  | P1 |
| P16 | 未开始 |  |  | P1 |
| P17 | 未开始 |  |  | P1 |
| P18 | 未开始 |  |  | P1 发布 |
| P19 | 未开始 |  |  | P2 |
| P20 | 未开始 |  |  | P2 |
| P21 | 未开始 |  |  | 最终交付 |
